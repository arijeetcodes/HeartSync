"""
Services contain the business logic.

For example,
When you press
1 → Add Memory
The menu should call: memory_service.py
instead of writing all the logic inside menu.py.

Example:
menu.py
↓
memory_service.py
↓
database.py
↓
database.json

This separation makes the code much easier to maintain.
"""